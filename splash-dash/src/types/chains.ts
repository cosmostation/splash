export type ChainType = {
  id: string;
  name: string;
  chainId: string;
  denom: string;
  decimal: number;
  dpDenom: string;
  geckoId: string;
  dashboardTitle: string;
  chainColor: string;
  apiURL: string;
  network: string;
  description?: string;
};
