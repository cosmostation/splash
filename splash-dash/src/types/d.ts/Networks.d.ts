export type Networks = {
  id: string;
  name: string;
  chainId: string;
  denom: string;
  decimal: number;
  dpDenom: string;
  geckoId: string;
  dashboardTitle: string;
  apiURL: string;
  network: string;
};
